% Figure: Tree → Graph → Tree  (HORIZONTAL, left-to-right, with a traced path)
% Concept: a local ERP 2D tree (left) is imported into Kontablo's
%          multi-dimensional graph (centre, isometric pseudo-3D); the graph is
%          linearized back into a 2D IFRS output tree (right). One example
%          account ("Cash") is traced in gold across all three stages so the
%          reader can follow a single account's journey through the protocol.
% Rendering: generated by figures/fig_tree_graph_tree.py (matplotlib).
%          To regenerate the PDF:
%              cd docs/papers/drafts && python3 figures/fig_tree_graph_tree.py
%          The old hand-built TikZ version is preserved (commented) at the
%          bottom of this file as an audit-trail fallback.
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,keepaspectratio]{figures/fig_tree_graph_tree.pdf}
\caption{Tree-to-Graph-to-Tree: the Kontablo linearization protocol, read left
to right. A local ERP chart of accounts (left, orange) is a rigid 2-D tree in
which each account has a single parent. On import (Tree-to-Graph), it enters
the Kontablo Universal Graph (centre, blue), where each account node carries a
UUID and participates simultaneously in multiple independent graph dimensions
--- IFRS/Balance Sheet (solid blue), Tax/Regulatory (dashed red), and
Functional/Cost-Centre (dotted violet); the centre is drawn in isometric
pseudo-3-D to make this multi-dimensionality explicit. For reporting or ERP
export, the graph is linearized (Graph-to-Tree) by selecting a single dimension
as the primary axis, producing a clean 2-D IFRS output tree (right, green)
without loss of the multi-dimensional data held in the graph. To make the
transformation legible, one example account (\emph{Cash}) is traced in gold
across all three stages: its local leaf \texttt{101 Cash}, its UUID node in the
graph, and its IFRS output \emph{Cash \& Equivalents}; the remaining accounts
are mapped in faint grey.}
\label{fig:tree_graph_tree}
\end{figure}

% ============================================================================
% SUPERSEDED: original hand-built TikZ version (horizontal layout).
% Kept for audit trail. The figure above (matplotlib-generated PDF) replaces it.
% ============================================================================
%
% \begin{figure}[p]
% \centering
% \resizebox{\textwidth}{!}{%
% \begin{tikzpicture}[ ... horizontal tree->graph->tree ... ]
%   (see git history at commit prior to the vertical redesign for full code)
% \end{tikzpicture}%
% }
% \end{figure}
