pandas
openpyxl
google-generativeai
fastapi
uvicorn
pydantic
pyyaml
python-dotenv
pytest
pytest-asyncio
httpx
jsonschema
requests
# gRPC interface (api/grpc): pinned to a protobuf-5.x-compatible line so it
# coexists with google-generativeai. Regenerate stubs with grpcio-tools when
# the proto changes (see api/grpc/README.md).
grpcio~=1.68.0
grpcio-tools~=1.68.0
# MCP interface (api/mcp): the official Model Context Protocol Python SDK
# (FastMCP). Exposes the deterministic engine as agent-consumable tools over
# stdio. No LLM key required for the deterministic core.
mcp>=1.10,<2
